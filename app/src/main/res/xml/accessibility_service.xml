<?xml version="1.0" encoding="utf-8"?>
<!--
    canRetrieveWindowContent: the words come from the node tree.
    canTakeScreenshot: the fallback for screens whose text is drawn, not reported.
    flagRequestAccessibilityButton: the system button is the trigger, so Taplex needs no
    bubble of its own and nothing of Taplex's can end up inside a capture.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRequestAccessibilityButton|flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:canTakeScreenshot="true"
    android:description="@string/a11y_description"
    android:notificationTimeout="100"
    android:settingsActivity="de.tieo.taplex.MainActivity" />
